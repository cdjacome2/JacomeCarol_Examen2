version: '3.8'

services:
  # Contenedor para MongoDB
  mongo:
    image: mongo:latest
    container_name: mongo
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin123
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network

  # Contenedor para el microservicio
  examen_jacome:
    build: .
    container_name: examen_jacome 
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://admin:admin123@mongo:27017/gestion_efectivo
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    networks:
      - app-network

# Volúmenes
volumes:
  mongo-data:

# Redes
networks:
  app-network:
    driver: bridge
